// @[./view/view_your_data.sx](from:now()[-7 day], to:now())
@[./default_query.sx]

| filter(Operation LIKE "%AntiPhish%")
| select(CreationTime,
         Parameters[AdminDisplayName],
         *
)
| sort(CreationTime DESC)
