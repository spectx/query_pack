$s3='s3://spectx-docs/logs/windows/Security.evtx';
$localhost= 'file:/C:/Windows/System32/winevt/Logs/Security.evtx';
@evtx=LIST([
	$s3,                        // Comment if you don't want to see example data.
// 	$localhost,                 // Uncomment if you want to query localhost events.
		   ]) 
| EVTX()
| join(@[./meta/EventID.sx] ON EventID)
| select(TimeCreated, EventID,Event_Description,*)
| select_remove(right_EventID,Source)
;

