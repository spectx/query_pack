@[./view/view_example_data.sx]
| filter(cs_uri_stem like '%shop%' or cs_uri_stem like '%sec%') 
| select(cs_uri_stem, c_ip, reqcount:count(cs_uri_stem), first_seen:min(time), last_seen:max(time)) 
| group(cs_uri_stem, c_ip )
| sort(reqcount desc)
